const config = Object.freeze({
  enum_preserved_subdomain: [
    "systemapi",
    "deviceapi",
    "tenantconnectionapi",
    "tenantapi",
    "system",
  ],
});

module.exports.service_subdomain = { config };
